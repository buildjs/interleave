================
Programmatic Use
================

While Interleave is designed to operate as a command-line tool in its own right, it can be combined with other tools such as `Jake`_ for more complicated build processes.

.. include:: links.txt
